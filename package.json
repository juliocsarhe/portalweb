{
  "name": "portal-roshkero",
  "private": true,
  "type": "module",
  "scripts": {
    "lint:commit": "commitlint --from=HEAD~1",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7"
  }
}
